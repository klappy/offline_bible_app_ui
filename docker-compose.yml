app:
  build: .
  command: nodemon server.js
  volumes:
    - .:/usr/src/app
  ports:
    - ':3000'
  environment:
    AUTH_HOST: https://staging-auth.sovee.com
  links:
    - db
    - redis
db:
  image: mongo:2.6
redis:
  image: redis:latest
